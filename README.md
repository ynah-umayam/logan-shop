# LoganShop

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 17.3.11.

## Requirements

### Node.js

This project requires **Node.js** version **18.x.x** or later.
You can check your installed version by running:
node -v

### npm

This project requires npm version **10.x.x** or later.
You can check your installed version by running:
npm -v

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

## Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via a platform of your choice. To use this command, you need to first add a package that implements end-to-end testing capabilities.

## Testing application

This application saves isUserAuthenticated into Local storage after a successful login using the credentials below. By logging out, the Local storage gets reset or you can reset it through Dev tools. When opening the application in Chrome, reset the Local storage by typing Option + ⌘ + J to Dev tools, go to Application tab and right click and click Clear for http://localhost:4200 under the Local storage.

email: abc@yahoo.com
password: Testpassw0rd!

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI Overview and Command Reference](https://angular.io/cli) page.
